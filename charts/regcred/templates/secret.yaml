{{- if .Values.dockerConfigJson.username }}
kind: Secret
type: kubernetes.io/dockerconfigjson
apiVersion: v1
metadata:
  name: {{ include "regcred.fullname" . }}
  {{- with .Values.extraLabels }}
  labels:
    {{- toYaml . | nindent 4 }}
  {{- end }}
data:
  .dockerconfigjson: {{- include "regcred.authSecret" ( dict "username" .Values.dockerConfigJson.username "password" .Values.dockerConfigJson.password ) | nindent 4 }}
{{- end }}
